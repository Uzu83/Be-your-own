# Be your own — 設計書

## 1. コンセプト

**自分が今まで積み上げてきた事実を、ただ可視化する。**

- ストリーク（連続日数）で焦らせない。「今日もやらないと途切れる」というプレッシャーを生まない。
- 評価しない。他人と比べない。順位をつけない。
- ただ事実を述べる。「あなたはこれだけ積んできた」。
- 積み上げた累積量を、身近で壮大なモノに**換算**して、努力の重みを実感させる。

> 例：あなたが今まで上げてきた総重量は **1,400 kg**。これは **GRヤリス 1台分**。

アプリ名「Be your own」＝ 基準は自分自身。自分の積み重ねだけを見る。

---

## 2. 対象アクティビティと累積する量

| アクティビティ | 累積する量 | 単位 |
| --- | --- | --- |
| 筋トレ | 総挙上重量（重量 × レップ × セット の総和） | kg |
| ランニング | 距離 / 時間 | km / 分 |
| 水泳 | 距離 / 時間 | km / 分 |
| ロードバイク | 距離 / 時間 | km / 分 |

- 種目ごとに「主指標」を持つが、距離系は **距離・時間の両方**を累積する。
- ユーザーはどの指標を主役に見せるか選べる（例：ランニングは距離派 / 時間派）。

---

## 3. 換算（このアプリの心臓部）

累積量を「身近で壮大なモノ N個分 / N周分」に翻訳する。

### 換算カテゴリ

| カテゴリ | 入力単位 | 換算例 |
| --- | --- | --- |
| 重量 (mass) | kg | GRヤリス1台 (≈1,280kg)、アフリカゾウ1頭 (≈6,000kg)、ジャンボ機 |
| 距離 (distance) | km | 地球1周 (40,075km)、東京–大阪 (≈400km)、富士山の高さ (3,776m)、フルマラソン (42.195km) |
| 時間 (time) | 分 | 映画1本 (≈120分)、フルマラソン世界記録、丸1日 |

### 換算ロジック

1. 累積量と単位カテゴリを受け取る。
2. そのカテゴリの「基準モノ」一覧から、**実感しやすい倍率**になるものを選ぶ。
   - 倍率が `1.0` を少し超えたあたり（「ちょうど〇個分を超えた」）が一番気持ちいい。
   - 小さすぎる（0.01個分）・大きすぎる（10,000個分）ものは避け、桁が手頃なものを選ぶ。
3. 「あなたが積んだ X = 〇〇 N個分」を返す。次のマイルストーン（あと少しで〇個目）も返す。

基準モノはデータとして外出しし、後から自由に追加できるようにする（`domain/conversion/references.ts`）。

---

## 4. データモデル

```
Activity        種目の定義（筋トレ / ランニング / 水泳 / ロードバイク）
  id, name, metricType（mass | distance | duration）, icon, color

Session         1回の記録
  id, activityId, date, value, unit, note?
  - 筋トレ: value = そのセッションの総挙上重量(kg)
  - 距離系: value = 距離(km) または 時間(分)

Cumulative      派生（Session を集計）
  activityId, totalValue, sessionCount, since（最初の記録日）
```

ローカル保存（AsyncStorage / SQLite）から開始。クラウド同期は将来。

---

## 5. 画面構成

ミニマリスト。白メイン・黒サブ。装飾を削ぎ、数字と換算を主役にする。

1. **Home（累積ダッシュボード）**
   - 各アクティビティの累積量を大きく表示。
   - その下に換算（「= GRヤリス1台分」）を控えめに。
   - スクロールでアクティビティを縦に並べる。
2. **Detail（種目詳細）**
   - 累積の内訳、これまでの換算履歴、次のマイルストーン。
3. **Record（記録入力）**
   - 種目を選び、数値を入れて保存。最小ステップで。
4. **Share（シェアカード）**
   - 累積量＋換算を1枚の画像に。白基調のミニマルなカード。SNSへ「カッコよく」共有。
   - 例：「総重量 1,400 kg ＝ GRヤリス1台分」を画像化して X / Instagram などへ。
   - 実装：画面外にカードを描画 → `react-native-view-shot` で画像化 → `expo-sharing` で共有。
5. **（将来）Settings** — 単位、表示する主指標の切替。

---

## 6. デザイン原則

- 配色：背景 `#FFFFFF` / 文字・主役 `#000000` / 補助グレー `#9E9E9E`。アクセント色は使わない。
- タイポgrafィ：数字を大きく、太く。ラベルは小さく、淡く。
- 余白を贅沢に。罫線や影は最小限。
- アニメーションは「数字が積み上がる」瞬間だけ控えめに。

---

## 7. 収益化・データ方針

- **基本無料**。誰でも全機能の核（記録・累積・換算・シェア）を使える。
- **広告付き**：控えめなバナー / 区切りでの表示。ミニマルな世界観を壊さない位置に限定。
  - 実装候補：`expo-ads-admob` 後継の AdMob 連携（`react-native-google-mobile-ads`）。
- **Ko-fi で応援可能**：「広告を消す」ではなく「作者を応援する」導線。Settings からリンク。
  - 将来は Ko-fi メンバーシップ連動で広告オフも検討。
- **データはローカル保存**：個人の積み重ねは端末内に置く。アカウント不要で始められる。
  - MVP は AsyncStorage、拡張で SQLite。クラウド同期はオプトインの将来機能。

---

## 8. 技術スタック

- React Native + Expo（TypeScript）
- 状態管理：軽量に（Zustand もしくは Context）
- 永続化：AsyncStorage（MVP）→ SQLite（拡張）
- ドメイン層（型・換算エンジン・基準データ）は UI から独立させ、純粋な TypeScript で実装・テスト可能にする。

---

## 9. 開発ステップ

- [x] 設計（本書）
- [ ] ドメイン層：型 + 換算エンジン + 基準データ
- [ ] Expo 土台 + 最小 Home 画面（換算表示）
- [x] 記録入力 + 永続化（ローカル）
- [x] シェアカード（画像化 → 共有）
- [x] 広告・Ko-fi 導線
- [x] 詳細画面・マイルストーン
